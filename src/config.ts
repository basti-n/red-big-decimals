export const DECIMALS = 18; // number of decimals on all instances
export const ROUNDED = true; // numbers are truncated (false) or rounded (true)
export const SHIFT = BigInt("1" + "0".repeat(DECIMALS)); // derived
